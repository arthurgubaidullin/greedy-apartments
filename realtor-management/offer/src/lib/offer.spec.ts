import { offerInRealtorManagement } from './offer';

describe('offerInRealtorManagement', () => {
  it('should work', () => {
    expect(offerInRealtorManagement()).toEqual('offer-in-realtor-management');
  });
});
