import { serviceInRealtorRegistry } from './service-in-realtor-registry';

describe('serviceInRealtorRegistry', () => {
  it('should work', () => {
    expect(serviceInRealtorRegistry()).toEqual('service-in-realtor-registry');
  });
});
