import { realtorInRegistry } from './realtor-in-registry';

describe('realtorInRegistry', () => {
  it('should work', () => {
    expect(realtorInRegistry()).toEqual('realtor-in-registry');
  });
});
