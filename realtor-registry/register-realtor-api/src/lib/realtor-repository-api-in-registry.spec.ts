import { realtorRepositoryApiInRegistry } from './realtor-repository-api-in-registry';

describe('realtorRepositoryApiInRegistry', () => {
  it('should work', () => {
    expect(realtorRepositoryApiInRegistry()).toEqual(
      'realtor-repository-api-in-registry'
    );
  });
});
