import { realtorStructInTenantManagement } from './realtor-struct-in-tenant-management';

describe('realtorStructInTenantManagement', () => {
  it('should work', () => {
    expect(realtorStructInTenantManagement()).toEqual(
      'realtor-struct-in-tenant-management'
    );
  });
});
