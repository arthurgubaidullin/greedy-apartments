import { offerRepositoryInRemoteOfferRegister } from './offer-repository-in-remote-offer-register';

describe('offerRepositoryInRemoteOfferRegister', () => {
  it('should work', () => {
    expect(offerRepositoryInRemoteOfferRegister()).toEqual(
      'offer-repository-in-remote-offer-register'
    );
  });
});
